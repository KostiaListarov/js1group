/*
 Сравнить null и 0. Попробуйте объяснить результат.
 */

alert(null == 0);   // false
alert(null > 0);    // false
alert(null < 0);    // false
alert(null >= 0);   // true
alert(null <= 0);   // true

/*
Интернет говорит что == и сравнения работают по разному алгоритму, но так особо и не понял как именно...
 */